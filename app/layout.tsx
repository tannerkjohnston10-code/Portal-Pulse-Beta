import type { Metadata } from "next";
import "./globals.css";
import { AppShell } from "@/components/layout/app-shell";

export const metadata: Metadata = {
  title: "Portal Pulse | NCAA Transfer Portal Intelligence",
  description: "Premium NCAA transfer portal intelligence dashboard for source-backed player movement, NIL, visits, predictions, and team rankings."
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <body>
        <AppShell>{children}</AppShell>
      </body>
    </html>
  );
}
