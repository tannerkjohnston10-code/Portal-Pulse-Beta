export const sourceSummarizer = {
  async summarizeSource(_text: string): Promise<string> {
    // Future summaries must be original, concise, source-linked, and must not reproduce copyrighted articles.
    return "";
  }
};
